<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WeatherApp.Views.LoaderView" Background="#80000000">
    <Grid>
        <ActivityIndicator Color="{x:StaticResource Primary}" WidthRequest="100" HeightRequest="100" IsRunning="True" HorizontalOptions="Center" VerticalOptions="Center"/>
    </Grid>
</ContentView>